# Integration Matrix

Evidence captured: 2026-05-19 09:40 CEST.

| Integration | Owner | Feature IDs | Sandbox | Fake | Mock | Outage | Retry | Rollback | Secret | Config Path | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Payment gateways: PayPal, Authorize.Net, Paygate | modernization | SF-015, API-004 | Enabled through `payment/paypal/sandbox`. | `Http::fake` covers failed payment response. | Payment webhook/IPN callback payload is mocked in `IntegrationFoundationTest`. | 500 response dispatches `RecoverIntegrationOutage`. | Adapter defaults use two retry attempts. | `legacy_runtime_fallback` is the configured rollback. | `services.payment_gateway.token` | `payment/paypal/sandbox` | Pass |
| Shipping carriers: UPS, USPS, FedEx, DHL | modernization | API-005, CJ-002 | Enabled through `carriers/ups/sandbox`. | `Http::fake` covers unavailable rate response. | Rate payload is mocked in `IntegrationFoundationTest`. | 503 response dispatches `RecoverIntegrationOutage`. | Adapter defaults use two retry attempts. | `legacy_runtime_fallback` is the configured rollback. | `services.shipping_carrier.api_key` | `carriers/ups/sandbox` | Pass |
| Currency rate imports | modernization | API-006, AD-018, CJ-004 | Enabled. | Adapter is covered by config registration. | Configured endpoint is deterministic. | Outage flow is covered by shared gateway. | Adapter defaults use two retry attempts. | `legacy_runtime_fallback` is the configured rollback. | `services.currency_rate.token` | `currency/import/service` | Pass |
| Google Analytics and Google Base | modernization | SF-016, API-006, CJ-017 | Enabled. | Adapter registration is covered by config test. | Configured endpoints are deterministic. | Outage flow is covered by shared gateway. | Adapter defaults use two retry attempts. | `legacy_runtime_fallback` is the configured rollback. | `services.google_analytics.token`, `services.google_base.token` | `google/analytics/account`, `google/base/account` | Pass |
| ERP, PIM, CRM, feed, webhook, email provider, OAuth | modernization | AD-018, API-006, API-004 | Enabled. | `Http::failedConnection` covers ERP failure. | Queue recovery snapshot is mocked in `IntegrationFoundationTest`. | Failed connection dispatches outage recovery. | Adapter defaults use two retry attempts; recovery job backoff is `[1, 5, 10]`. | `legacy_runtime_fallback` is the configured rollback. | Service-specific token, password, signature, and OAuth keys. | `project/erp/api`, `project/pim/api`, `project/crm/api`, `project/feed/api`, `payment/webhook/signature`, `system/smtp/host`, `oauth/consumer/key` | Pass |

